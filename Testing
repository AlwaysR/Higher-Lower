import math

def calc_guesses(low, high):
    pass

to_test = [
    (1, 10, 5),
    (1, 20 ,6),
    (1, 100, 8),
    (1, 1000, 11),
]

for item in to_test:
    low_num = item[0]
    high_num = item[1]
    expected = item[2]

    actual = calc_guesses(low_num, high_num)

    if actual = expected:

#53secs into ep28
